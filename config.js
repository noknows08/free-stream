// Paste your Supabase project values here to enable accounts + watchlist.
// Leave blank to run the site with the built-in catalog only (still fully works).
window.SUPABASE_URL = "";
window.SUPABASE_ANON_KEY = "";
